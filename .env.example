# Create a .env file locally to override the backend URL if you are testing the backend locally.
# VITE_API_BASE_URL=http://localhost:4000

# In production (on Vercel), the code automatically falls back to:
# VITE_API_BASE_URL=https://kishan-classes-backend.onrender.com
# So you do NOT need to add any environment variables in Vercel!
